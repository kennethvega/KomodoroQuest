import React from "react";
import styles from "./Footer.module.scss";
function Footer() {
  return (
    <section className={styles.footer}>
      <h2>Design and built by Kenneth Vega </h2>
    </section>
  );
}

export default Footer;
